package valtecex;
/*
public class Pangrams99 {

    // static String isPangram(String[] strings) {

    public static String[] isPangram(String[] check)
    {
        String result = "";
        for(String data : check){
            result += checkPangram(data);
        }
        return check;
    }

    public static int checkPangram(String test){
        for (char a = 'A'; a <= 'Z'; a++)
            if ((test.indexOf(a) < 0) && (test.indexOf((char)(a + 32)) < 0))
                return 0;
        return 1;
    }
    //the quick brown fox jump over the lazy dog


    public static void main(String[] args) throws IOException {
        BufferedReader br = new BufferedReader(new InputStreamReader(System.in));
        String word = br.readLine();
        String[] array = new String[word. length()];
        for(int i = 0; i < word. length(); i++)
        {
            array[i] = String. valueOf(word. charAt(i));
        }
        //String[] input=new String[br.readLine()];
        System.out.println(checkPangram(array));  //check Pangram for the given input by user
        //if(check == 1)
          //  System.out.println("pangram");
        //else
          //  System.out.println("not pangram");
    }
}

*/